/**
 * Phase 1 of the unified dual-sync engine.
 *
 * Profile section field configs.
 */

import {
  canonicalizePhone,
  canonicalizeText,
  canonicalizeUrl,
  normalizePhone,
  normalizeString,
  normalizeUrl,
} from './normalizers';

import type { DualSyncFieldConfig } from './types';

export const PROFILE_FIELDS: ReadonlyArray<DualSyncFieldConfig<string | null, string | null>> = [
  {
    fieldKey: 'profile.name',
    sectionKey: 'profile',
    kind: 'profile',
    label: 'Business name',
    helpText: 'Public restaurant name shown across Google and the guest site.',
    corePath: 'restaurant.name',
    gbpPath: 'location.title',
    importable: true,
    exportable: true,
    normalizeCoreValue: normalizeString,
    normalizeGbpValue: normalizeString,
    canonicalizeCoreValue: canonicalizeText,
    canonicalizeGbpValue: canonicalizeText,
    conflictPolicy: 'manual',
    deletePolicy: 'manual',
    googleUpdateMask: 'title',
    exportRequiresCoreValue: true,
    sortOrder: 0,
  },
  {
    fieldKey: 'profile.businessDescription',
    sectionKey: 'profile',
    kind: 'profile',
    label: 'Business description',
    helpText: 'Long-form description shown on the Google profile and guest site.',
    corePath: 'restaurant.businessDescription',
    gbpPath: 'location.profile.description',
    importable: true,
    exportable: true,
    normalizeCoreValue: normalizeString,
    normalizeGbpValue: normalizeString,
    canonicalizeCoreValue: normalizeString,
    canonicalizeGbpValue: normalizeString,
    conflictPolicy: 'manual',
    deletePolicy: 'clear_remote',
    googleUpdateMask: 'profile',
    sortOrder: 1,
  },
  {
    fieldKey: 'profile.contactPhone',
    sectionKey: 'profile',
    kind: 'profile',
    label: 'Contact phone',
    helpText: 'Primary public phone number.',
    corePath: 'restaurant.contactPhone',
    gbpPath: 'location.phoneNumbers.primaryPhone',
    importable: true,
    exportable: true,
    normalizeCoreValue: normalizePhone,
    normalizeGbpValue: normalizePhone,
    canonicalizeCoreValue: canonicalizePhone,
    canonicalizeGbpValue: canonicalizePhone,
    conflictPolicy: 'manual',
    deletePolicy: 'manual',
    googleUpdateMask: 'phoneNumbers',
    exportRequiresCoreValue: true,
    sortOrder: 2,
  },
  {
    fieldKey: 'profile.address',
    sectionKey: 'profile',
    kind: 'profile',
    label: 'Address',
    helpText: 'Storefront address. Export requires existing Google structured-address metadata.',
    corePath: 'restaurant.address',
    gbpPath: 'location.storefrontAddress',
    importable: true,
    exportable: true,
    normalizeCoreValue: normalizeString,
    normalizeGbpValue: normalizeString,
    canonicalizeCoreValue: canonicalizeText,
    canonicalizeGbpValue: canonicalizeText,
    conflictPolicy: 'manual',
    deletePolicy: 'manual',
    googleUpdateMask: 'storefrontAddress',
    requiresGoogleCapability: 'storefrontAddress.regionCode',
    exportRequiresCoreValue: true,
    exportBlockedReason:
      'Address export requires an existing Google storefrontAddress with a region code.',
    sortOrder: 3,
  },
  {
    fieldKey: 'profile.googleMapUrl',
    sectionKey: 'profile',
    kind: 'profile',
    label: 'Google Maps URL',
    helpText: 'Output-only metadata generated by Google. Import-only.',
    corePath: 'restaurant.googleMapUrl',
    gbpPath: 'location.metadata.mapsUri',
    importable: true,
    exportable: false,
    normalizeCoreValue: normalizeUrl,
    normalizeGbpValue: normalizeUrl,
    canonicalizeCoreValue: canonicalizeUrl,
    canonicalizeGbpValue: canonicalizeUrl,
    conflictPolicy: 'gbp_wins',
    deletePolicy: 'ignore',
    exportBlockedReason: 'Google Maps URL is Google-owned metadata and is not directly writable.',
    sortOrder: 4,
  },
  {
    fieldKey: 'profile.googleReviewUrl',
    sectionKey: 'profile',
    kind: 'profile',
    label: 'Google review URL',
    helpText: 'Output-only metadata generated by Google. Import-only.',
    corePath: 'restaurant.googleReviewUrl',
    gbpPath: 'location.metadata.newReviewUri',
    importable: true,
    exportable: false,
    normalizeCoreValue: normalizeUrl,
    normalizeGbpValue: normalizeUrl,
    canonicalizeCoreValue: canonicalizeUrl,
    canonicalizeGbpValue: canonicalizeUrl,
    conflictPolicy: 'gbp_wins',
    deletePolicy: 'ignore',
    exportBlockedReason: 'Google review URL is Google-owned metadata and is not directly writable.',
    sortOrder: 5,
  },
] as const;
