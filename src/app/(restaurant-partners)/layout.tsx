import type { ReactNode } from "react";

type RestaurantPartnersLayoutProps = {
  children: ReactNode;
};

export default function RestaurantPartnersLayout({ children }: RestaurantPartnersLayoutProps) {
  return <>{children}</>;
}
