import { RoleSelectionLayout } from '@/components/layouts/RoleSelectionLayout';

export default function Layout({ children }: { children: React.ReactNode }) {
  return <RoleSelectionLayout>{children}</RoleSelectionLayout>;
}
