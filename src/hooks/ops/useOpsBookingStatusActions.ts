/**
 * Compatibility entry point (re-exported by the frozen `src/hooks/index.ts` barrel). The four
 * lifecycle wrappers were consolidated into `useBookingLifecycle`; import it from
 * `@src/hooks/ops/useBookingLifecycle` directly.
 */
export * from './useBookingLifecycle';
