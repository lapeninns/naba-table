# Archived Supabase Migrations

This folder preserves deprecated or dump-style migrations that were replaced by the
cleaned schema baseline. They remain in git history for traceability but are excluded
from the active migration chain.

- `20251020235523_remote_schema.sql`
- `20251026160304_remote_schema.sql`
- `20251026160713_remote_schema.sql`
- `20251101093000_create_strategic_configs.sql`

If the team agrees these are no longer needed, delete them after confirming no
production environments rely on them.
