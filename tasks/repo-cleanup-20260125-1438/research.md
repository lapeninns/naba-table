---
task: repo-cleanup
timestamp_utc: 2026-01-25T14:38:16Z
owner: github:@maintainers
reviewers: [github:@maintainers]
risk: low
flags: []
related_tickets: []
---

# Research: Repo cleanup (artifacts + unused code)

## Requirements

- Functional:
  - Remove unwanted files/folders/components from the repo on a new branch.
  - Focus on artifacts and unused code/components.
  - Keep active source and documentation intact.
- Non-functional (a11y, perf, security, privacy, i18n):
  - Security: remove tracked secret-like files (local envs, dumps) and prevent reintroduction via .gitignore.
  - No UI changes required; no a11y/perf impact expected.

## Existing Patterns & Reuse

- .gitignore already covers env files, .vercel, node_modules, build outputs, and test artifacts.
- Repo uses both root and src directories with aliasing in tsconfig; cleanup must respect path aliases.
- Route artifacts appear to be generated by `route-scanner.js` (route-map JSON output).

## External Resources

- None required.

## Constraints & Risks

- Follow AGENTS SDLC phases; do not delete files before plan is documented.
- Multiple AGENTS.md files exist; deletions must respect scoped rules.
- Risk: deleting files still referenced by scripts/build or docs.
- Docs reference `route-map-mermaid.md` and `route-map-ascii.txt`; keep them unless docs updated.

## Candidate Artifact Removals (no references found via rg)

- Root JSON/code dumps: `booking_details_codebase.json`, `booking_details_dialog_code.json`, `booking_response.json`, `dashboard_codebase.json`, `floor_plan_code.json`, `landing_page_code.json`, `session_summary.json`.
- Root misc: `b2b-landing-content.json`, `restaurant.json`, `progress.txt`, `referenceofbookingdetails.js`, `email-templates-showcase.html`, `next` (empty file).
- Generated route data: `route-map.json` (output of `route-scanner.js`).
- Backups/dumps: `backups/` directory and `db_cluster-27-12-2025@05-23-04.backup.gz`.
- Context folder: `context/` (JSON dumps).
- Legacy components/folders with 0-hit searches: `components/atoms`, `components/guest-account`, `components/invite`, `components/layout`, `components/marketing`, `components/owner-marketing`, `components/profile`, `components/reserve/helpers.ts`, `components/reserve/steps/*`, `components/mobile/*`, `components/reserve/booking-flow/*`.
- Reserve shared modules with 0-hit searches: `reserve/shared/utils/booking.ts`, `reserve/shared/schedule/availability.ts`, `reserve/shared/ui/Field.tsx`.
- Scratch files: `src/app/layout.tsx.new`, `src/components/common/index.ts`, `src/app/globals.css.minimal-backup`.

## Open Questions (owner, due)

- None; assume only obvious artifacts and verified-unused code/components are removed.

## Recommended Direction (with rationale)

- Phase 1: remove clearly generated or local-only artifacts (node_modules, backups, dumps, tsbuildinfo, local envs, .vercel). This is low risk and aligns with security constraints.
- Phase 2: remove unused code/components only when usage is proven absent via rg and not referenced by scripts/docs.
- Update .gitignore only if missing patterns for artifacts identified.
