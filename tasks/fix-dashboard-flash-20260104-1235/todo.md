# Implementation Checklist

## Core

- [x] Comment out `OpsOfflineIndicator` in `OpsSidebarLayout.tsx`.
- [x] Fix layout redirect from `/auth` to `/auth/signin`.

## Verification

- [ ] User verifies no flash and no error on page load.
