# Tooling

Important tooling lives in `package.json`, `scripts/**`, `.github/workflows/shadcn-primitives.yml`, `.husky/pre-commit`, and `.factory/skills/**`. Key scripts include `scripts/check-no-shadcn.mjs`, `scripts/check-luma-compliance.mjs`, `scripts/validate-env.ts`, and `scripts/db/safe-run.ts`.

Related: [Testing](testing.md), [Deployment](../deployment.md).
