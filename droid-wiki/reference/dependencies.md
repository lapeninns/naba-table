# Dependencies

Dependencies are declared in `package.json` and locked in `pnpm-lock.yaml`. Runtime groups include `next`, `react`, `@supabase/*`, `@radix-ui/*`, `@tanstack/react-query`, `resend`, and analytics packages. Development groups include `vitest`, `@playwright/test`, `vite`, `storybook`, `eslint`, `prettier`, and `typescript`.

Related: [Dependency freshness](../cleanup-opportunities/dependency-freshness.md).
