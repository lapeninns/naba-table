# Configuration

Configuration is parsed by `config/env.schema.ts` and `lib/env.ts`, checked by `scripts/validate-env.ts`, and documented in `.env.example` and `README.md`. Important groups include app host variables, Supabase keys, safety markers, delivery keys, and feature flags.

Related: [Feature flags](../primitives/feature-flags.md).
