# API

API pages cover [Public API](public-api.md), [Ops API](ops-api.md), [Webhooks and cron](webhooks-cron.md), and [Auth and profile](auth-profile.md). The current snapshot has 140 tracked `src/app/api/**/route.ts` handlers and 156 tracked TS/TSX files under `src/app/api/**`.
