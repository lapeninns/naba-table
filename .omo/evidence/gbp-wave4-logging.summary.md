# GBP Wave 4 structured logging verification

- Scenario: cron failures for dual-sync auto-export, queue drain, and core outbox return their existing terminal 500 responses while emitting only structured metadata (`source`, `runId`, `errorKind`) and retaining sanitized exception telemetry. Invocation: `./node_modules/.bin/vitest run tests/server/dual-sync-auto-export-cron-route.test.ts tests/server/dual-sync-queue-cron-route.test.ts tests/server/dual-sync-core-outbox-route.test.ts tests/server/dual-sync-notifications.test.ts tests/server/dual-sync-locks.test.ts tests/server/google-business-profile/food-menus-import-decisions.test.ts --reporter=verbose`. Binary observable: `Test Files 6 passed`, `Tests 40 passed`. Artifact: `gbp-wave4-logging.focused-tests.log`.
- Scenario: webhook transport failures preserve their ambiguous outcome and reach the injectable console notification port without provider exception text. The same focused test invocation passed; the test asserts the typed `cron_run_failed` event and confirms that a token/email fixture is absent. Artifact: `gbp-wave4-logging.focused-tests.log`.
- Scenario: a dual-sync lock-release failure and a GBP food-menu review failure-transition failure retain original error/retry behavior while logging safe metadata only. The same focused test invocation passed; the tests confirm original error propagation and absence of the token/email fixture. Artifact: `gbp-wave4-logging.focused-tests.log`.
- Scenario: type safety. Invocation: `./node_modules/.bin/tsc --noEmit`. Binary observable: `EXIT_CODE: 0`. Artifact: `gbp-wave4-logging.typecheck.log`.
- Scenario: scoped lint. Invocation: `./node_modules/.bin/eslint <owned runtime and focused test files>`. Binary observable: `EXIT_CODE: 0`. Artifact: `gbp-wave4-logging.lint.log`.
- Scenario: scoped formatting. Invocation: `./node_modules/.bin/prettier --check <owned runtime and focused test files>`. Binary observable: `All matched files use Prettier code style!`. Artifact: `gbp-wave4-logging.prettier.log`.
- Scenario: runtime logging boundary scan. Invocation: `rg` scans over all six owned runtime files. Binary observable: no `console.*` calls and no raw exception-message logging metadata; the only `captureServerException` calls are immediately preceded by their sanitizer-boundary documentation. Artifact: `gbp-wave4-logging.live-source-scan.log`.
- Scenario: diff whitespace check. Invocation: `git diff --check` for tracked changes and `git diff --no-index --check` for the two additive tests. Binary observable: no diagnostics; tracked check exit `0`. Artifact: `gbp-wave4-logging.diff-check.log`.
